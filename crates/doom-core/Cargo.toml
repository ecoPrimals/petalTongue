[package]
name = "doom-core"
version = "0.1.0"
edition = "2021"
authors = ["petalTongue Evolution Team"]
description = "Doom integration for petalTongue platform"
license = "AGPL-3.0"

[dependencies]
# Core dependencies
thiserror = "1.0"
tracing = "0.1"
anyhow = "1.0"

# Pure Rust - no external WAD parsing deps!
# We implement our own WAD parser for full control

# For rendering (later phases)
# glam = "0.24"  # Math library for 3D

[dev-dependencies]

